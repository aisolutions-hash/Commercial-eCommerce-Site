# Single-container: API at /api on same origin, so VITE_API_BASE_URL not needed
# (defaults to "/api" in api.ts). Set only if dev API differs from Vite proxy.
# Dev: Vite proxy routes /api -> localhost:8000 (see vite.config.ts)
# VITE_API_BASE_URL=http://localhost:8000/api
